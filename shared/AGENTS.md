# Git

- NEVER add co-authored trailers (e.g. `Co-Authored-By: Claude ...`) to commit messages.
- NEVER add "Generated with Claude Code" attribution or any AI co-authorship footers to PR descriptions.

# Tools

- Prefer `rg` (ripgrep) over `grep` for searching when it is installed; fall back to `grep` if it is not available.
